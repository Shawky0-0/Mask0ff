interface:
  display_name: "mask0ff Security Research"
  short_description: "Tool-led research with adversarial validation"
  default_prompt: "Use $mask0ff to import authorized scope, map attacker-controlled trust transitions, run tool-led research, and require independent X1 reproduction plus adversarial J1 triage before reporting."
