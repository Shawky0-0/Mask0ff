interface:
  display_name: "mask0ff Security Research"
  short_description: "Dynamic authorized research with verified evidence"
  default_prompt: "Use $mask0ff to import this program scope, use any supplied researcher session safely, select black/gray/white-box mode, and continue evidence-first until a terminal finding state."
